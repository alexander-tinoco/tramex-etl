# API
fastapi>=0.141.1
uvicorn[standard]>=0.52.3
python-multipart>=0.0.32

# Persistencia
SQLAlchemy>=2.0.52
psycopg2-binary>=2.9.12
alembic>=1.19.1

# Configuracion y validacion
pydantic[email]>=2.13.4
pydantic-settings>=2.15.0

# Seguridad
cryptography>=50.0.0            # Fernet: cifrado de credenciales de clientes
python-jose[cryptography]>=3.5.0  # Firma y verificacion de los JWT de sesion
bcrypt>=5.0.0                   # Hash de contrasenas de usuarios del sistema
redis>=8.1.0                    # Contadores de fuerza bruta compartidos

# Observabilidad
sentry-sdk[fastapi]>=2.68.0
prometheus-client>=0.26.0

# El paquete `shared/` (reglas de identidad comunes con el ETL) NO se declara
# aqui: su ruta relativa cambia segun desde donde se instale (raiz del
# repositorio en local, /app en la imagen). Se instala como paso aparte:
#   pip install -e ./shared      # desarrollo, desde la raiz del repositorio
#   pip install ./shared         # imagen de Docker

# Pruebas y calidad
pytest>=9.1.1
pytest-cov>=7.1.0
httpx>=0.28.1
ruff>=0.16.3
mypy>=2.3.1
