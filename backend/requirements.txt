# API
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.12

# Persistencia
SQLAlchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13

# Configuracion y validacion
pydantic[email]>=2.0
pydantic-settings>=2.2

# Seguridad
cryptography>=42.0            # Fernet: cifrado de credenciales de clientes
python-jose[cryptography]>=3.3  # Firma y verificacion de los JWT de sesion
bcrypt>=4.1                   # Hash de contrasenas de usuarios del sistema
redis>=5.0                    # Contadores de fuerza bruta compartidos

# Observabilidad
sentry-sdk[fastapi]>=2.0.0
prometheus-client>=0.20

# Reglas de identidad compartidas con el ETL
-e ../shared

# Pruebas y calidad
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
ruff>=0.6
mypy>=1.11
